import Icon from "../Icon";

const style = {
  color: "#22c55e",
  fontSize: "36px",
};

const MarkerIcon = () => (
  <div style={style}>
    <Icon icon="location-dot" />
  </div>
);

export default MarkerIcon;
