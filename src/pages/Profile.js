import styled from "styled-components";

const Profile = () => {
  return (
    <Container>
      <h1>Profile</h1>
    </Container>
  );
};

const Container = styled.div`
  max-width: 1200px;
  margin: 0 auto;
  padding: 10px 20px;
`;

export default Profile;
